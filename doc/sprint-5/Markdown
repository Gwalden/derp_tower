Ce que l'on a réussi :

On a réussi a s'inscrire dans la base de donnée via une requête ajax venant d'une page HTTP

Ce que l'ont a appris :

Utiliser le DAO et la BDDfactory , ...

Ce que l'on va faire au prochain sprint:

Le log in